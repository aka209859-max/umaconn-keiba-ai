# CEO環境での動作確認テスト手順

**作成日**: 2026-01-10  
**対象**: grade_code統合後の動作確認  
**実行環境**: Windows (E:\UmaData\nar-analytics-python-v2)

---

## 📋 事前準備

### 1. 最新コードの取得

```cmd
E:
cd \UmaData\nar-analytics-python-v2
git pull origin main
```

**期待結果**:
```
Updating e3096bd..827a9c0
Fast-forward
 core/index_calculator.py                 |   9 +-
 docs/zenhan_3f_calculation_methods.md    | 620 ++++++++++++++++++++++
 docs/zenhan_3f_quick_reference.md        | 291 ++++++++++
 tests/test_grade_code_integration.py     | 335 ++++++++++++
 4 files changed, 1252 insertions(+), 3 deletions(-)
```

---

## 🧪 テストケース一覧

### テスト1: 実測値使用（基本動作確認）
### テスト2: 推定値使用（E級、1600m）
### テスト3: 推定値使用（上位クラス、1600m）
### テスト4: 1200m以下の前半タイム計算
### テスト5: 1000mの前半タイム計算（約2F）

---

## 🚀 テスト実行方法

### 方法A: 統合テストスクリプト実行（推奨）

```cmd
python tests\test_grade_code_integration.py
```

**期待結果**:
```
============================================================
テスト結果サマリー
============================================================
✅ テスト1: 実測値使用 - 成功
✅ テスト2: 推定値使用（E級） - 成功
✅ テスト3: 推定値使用（上位クラス） - 成功
✅ テスト4: 1200m以下計算 - 成功
✅ テスト5: 1000m計算 - 成功

✅ 全テスト合格！
```

---

### 方法B: 個別コマンド実行（詳細確認）

#### テスト1: 基準タイム確認（E級 vs 上位クラス）

```cmd
python -c "from config.base_times import get_base_time; e = get_base_time('30', 1600, 'zenhan_3f', 'E'); a = get_base_time('30', 1600, 'zenhan_3f', 'A'); print(f'E級: {e}秒, A級: {a}秒, 差分: {a-e:.1f}秒')"
```

**期待結果**:
```
E級: 35.8秒, A級: 36.0秒, 差分: 0.2秒
```

---

#### テスト2: 推定値使用（E級、門別1600m）

```cmd
python -c "from core.index_calculator import calculate_all_indexes; result = calculate_all_indexes({'zenhan_3f': None, 'kohan_3f': 395, 'corner_1': 5, 'corner_2': 5, 'corner_3': 5, 'corner_4': 5, 'kyori': 1600, 'babajotai_code_dirt': '1', 'keibajo_code': '30', 'tosu': 12, 'furi_code': '00', 'wakuban': 5, 'kinryo': 54.0, 'bataiju': 460.0, 'soha_time': 1050}, {'grade_code': 'E'}); print(f'推定前半3F: {result[\"estimated_ten_3f\"]:.2f}秒, 方法: {result[\"ten_3f_method\"]}, テン指数: {result[\"ten_index\"]:.1f}')"
```

**期待結果**:
```
推定前半3F: 35.80秒, 方法: adjusted, テン指数: 0.2
```

---

#### テスト3: 推定値使用（上位クラス、門別1600m）

```cmd
python -c "from core.index_calculator import calculate_all_indexes; result = calculate_all_indexes({'zenhan_3f': None, 'kohan_3f': 391, 'corner_1': 3, 'corner_2': 3, 'corner_3': 3, 'corner_4': 3, 'kyori': 1600, 'babajotai_code_dirt': '1', 'keibajo_code': '30', 'tosu': 12, 'furi_code': '00', 'wakuban': 3, 'kinryo': 54.0, 'bataiju': 460.0, 'soha_time': 1040}, {'grade_code': 'A'}); print(f'推定前半3F: {result[\"estimated_ten_3f\"]:.2f}秒, 方法: {result[\"ten_3f_method\"]}, テン指数: {result[\"ten_index\"]:.1f}')"
```

**期待結果**:
```
推定前半3F: 36.00秒, 方法: adjusted, テン指数: -0.2
```

---

#### テスト4: 1200m以下（T_total - T_last）

```cmd
python -c "from core.index_calculator import calculate_all_indexes; result = calculate_all_indexes({'zenhan_3f': None, 'kohan_3f': 360, 'corner_1': 5, 'corner_2': 5, 'corner_3': 5, 'corner_4': 5, 'kyori': 1200, 'babajotai_code_dirt': '1', 'keibajo_code': '30', 'tosu': 12, 'furi_code': '00', 'wakuban': 5, 'kinryo': 54.0, 'bataiju': 460.0, 'soha_time': 720}, {'grade_code': 'E'}); expected = 72.0 - 36.0; actual = result['estimated_ten_3f']; print(f'推定前半タイム: {actual:.2f}秒, 期待値: {expected:.2f}秒, 一致: {abs(actual - expected) < 0.1}')"
```

**期待結果**:
```
推定前半タイム: 36.00秒, 期待値: 36.00秒, 一致: True
```

---

#### テスト5: 1000m（前半タイム ≈ 2F）

```cmd
python -c "from core.index_calculator import calculate_all_indexes; result = calculate_all_indexes({'zenhan_3f': None, 'kohan_3f': 365, 'corner_1': 5, 'corner_2': 5, 'corner_3': 5, 'corner_4': 5, 'kyori': 1000, 'babajotai_code_dirt': '1', 'keibajo_code': '30', 'tosu': 12, 'furi_code': '00', 'wakuban': 5, 'kinryo': 54.0, 'bataiju': 460.0, 'soha_time': 605}, {'grade_code': 'E'}); expected = 60.5 - 36.5; actual = result['estimated_ten_3f']; print(f'推定前半タイム: {actual:.2f}秒（約2F）, 期待値: {expected:.2f}秒, 一致: {abs(actual - expected) < 0.1}')"
```

**期待結果**:
```
推定前半タイム: 24.00秒（約2F）, 期待値: 24.00秒, 一致: True
```

---

## 📊 クラス別基準タイムの検証

### 門別1600m（全クラス比較）

```cmd
python -c "from config.base_times import get_base_time; classes = [('A', '上位'), ('B', '上位'), ('C', '上位'), ('D', '上位'), ('E', 'E級'), (None, '一般')]; [print(f'{desc:<6} ({code if code else \"一般\":<4}): {get_base_time(\"30\", 1600, \"zenhan_3f\", code):.1f}秒') for code, desc in classes]"
```

**期待結果**:
```
上位   (A   ): 36.0秒
上位   (B   ): 36.0秒
上位   (C   ): 36.0秒
上位   (D   ): 36.0秒
E級    (E   ): 35.8秒
一般   (一般 ): 35.8秒
```

---

### 盛岡1600m（全クラス比較）

```cmd
python -c "from config.base_times import get_base_time; classes = [('A', '上位'), ('B', '上位'), ('C', '上位'), ('D', '上位'), ('E', 'E級'), (None, '一般')]; [print(f'{desc:<6} ({code if code else \"一般\":<4}): {get_base_time(\"35\", 1600, \"zenhan_3f\", code):.1f}秒') for code, desc in classes]"
```

**期待結果**:
```
上位   (A   ): 36.8秒
上位   (B   ): 36.8秒
上位   (C   ): 36.8秒
上位   (D   ): 36.8秒
E級    (E   ): 37.0秒
一般   (一般 ): 37.0秒
```

---

## ✅ 成功基準

### 1. 全テスト合格
- ✅ テスト1〜5がすべて成功

### 2. クラス別基準タイムが正しく使用される
- ✅ E級: 35.8秒（門別1600m）
- ✅ A級: 36.0秒（門別1600m）
- ✅ 差分: 0.2秒

### 3. 距離別計算が正しく動作する
- ✅ 1201m以上: Ten3FEstimator推定（基準タイム + スピード指数補正）
- ✅ 1200m以下: `T_total - T_last`（確定値）
- ✅ 1000m: 24.0秒（約2F）

### 4. 推定方法が正しく記録される
- ✅ `method='actual'`: 実測値使用
- ✅ `method='adjusted'`: 基準タイム + スピード指数補正
- ✅ `method='baseline'`: 1200m以下の確定値計算

---

## 🔍 トラブルシューティング

### エラー1: `ModuleNotFoundError: No module named 'config'`

**原因**: プロジェクトルートで実行していない

**対処法**:
```cmd
E:
cd \UmaData\nar-analytics-python-v2
# ここでコマンドを実行
```

---

### エラー2: `KeyError: 'estimated_ten_3f'`

**原因**: 実測値が存在する場合は `estimated_ten_3f` が返されない

**対処法**: 正常動作です。実測値使用時は推定値は不要です。

---

### エラー3: git pull でエラー

**原因**: ローカル変更とコンフリクト

**対処法**:
```cmd
git stash
git pull origin main
git stash pop
```

---

## 📝 結果報告フォーマット

CEO、以下の形式で結果を報告してください：

```
【テスト結果】
✅/❌ テスト1: 実測値使用
✅/❌ テスト2: 推定値使用（E級）
✅/❌ テスト3: 推定値使用（上位クラス）
✅/❌ テスト4: 1200m以下計算
✅/❌ テスト5: 1000m計算

【クラス別基準タイムの確認】
門別1600m E級: XX.X秒
門別1600m A級: XX.X秒
差分: X.X秒

【総合評価】
✅/❌ 全テスト合格
✅/❌ クラス別基準タイム正常
✅/❌ 距離別計算正常

【備考】
（気になる点や質問があれば記載）
```

---

## 🚀 次のステップ

テスト合格後:
1. **本番データで検証**: 実際のレースデータで推定精度を確認
2. **HQS指数計算**: 4つの指数（テン/位置/上がり/ペース）を統合
3. **予想精度評価**: 的中率・回収率の向上を確認

---

**作成者**: Enable CEO & AI戦略家  
**Play to Win!** 🚀
